import React, { Component }  from 'react';
import styles from './Loading.scss';

export default class Loading extends Component {
    render() {
        return (
            <div id="loadingFile">
                <a href="#" className="link">Loading...</a>
            </div>
        );
    }
}